inherit_from: configs/experiments/full_fast_cold_disease_pairfeat_cuda_b128_interaction_cached_posonly.yaml

model:
  path_scorer:
    binary_head:
      enabled: true
      hidden_dim: 128
      dropout: 0.1

training:
  path_binary_aux:
    enabled: true
    weight: 0.1
    use_separate_head: true
    negative_sources:
      - corrupt_internal
      - cross_pair_same_schema
